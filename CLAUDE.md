# 電車 乗り換えチャレンジ

電車の乗り換えが時間内に成功するかを遊ぶ、ブラウザ用のゲームです。
もとは「横浜駅 乗り換えチャレンジ」（新横浜→横浜の1駅固定）でしたが、首都圏の複数の駅から選べる
汎用ゲームに再構築しました。iPhoneのSafariで遊ぶことを一番に考えています。ホーム画面に追加して使えるPWAになっている。

## ファイル
- index.html：ゲーム本体のHTML（骨組みのみ）
- style.css：見た目（色・レイアウト・ダークモード対応）
- script.js：駅データ・ゲームのロジック・描画・当たり判定など全部
- analytics.js：アクセス計測（下記「アクセス計測」を参照）。ゲームのロジックとは完全に分離している
- manifest.webmanifest：PWAの設定（アプリ名・アイコン・テーマ色など）
- sw.js：オフラインでも起動できるようにするService Worker（キャッシュのみ、通信は増やさない）。
  キャッシュ名は `norikae-transfer-v3`。ファイル内容を大きく変えたらこのバージョン文字列を上げてキャッシュを更新する。
  **これを忘れると、既にアクセスしたことのある人には古いファイルが配信され続ける**
- icons/：ホーム画面アイコン（icon-192.png / icon-512.png / apple-touch-icon.png / favicon-32.png）

## ゲームの流れ
1. モードを選ぶ（通常モード／タイムトライアル）
2. 駅を選ぶ（レベル1〜50。レベルが高いほど乗り換え時間が短く・混雑が激しく・遅れも増える）
3. 乗ってきた電車の号車を選ぶ（レベル30以下は駅の地図を先に見られる。31以上は着いてから）
4. 選んだ駅の入線区間を移動（途中の停車駅でランダムに遅れが出る）
5. 駅の中を歩いて、乗り換え先の電車のドアまで行く
   - 通常モード：発車時刻までに着けば成功。間に合わなければ失敗
   - タイムトライアル：発車は気にせず、乗り換えにかかった時間（S.elapsed）を計測し、駅ごとの自己ベストをlocalStorageに記録

## 駅データとレベル設計（script.js内 STATIONS / diffFor）
- `STATIONS` 配列に首都圏の実在駅・実在路線名を使った乗り換え例を26件収録（レベル1〜50に分散）。
  時刻・停車駅間隔・駅構内の配置・混雑・遅れは実際とは異なる、ゲーム用にデフォルメした値。
- 各駅は `{id, level, from, to, fromLine:{name,color}, toLine:{name,color}, stops[], rideMinutes}` を持つ。
- 難易度（乗り換え時間の余裕・混雑の出やすさ・遅延の大きさ・NPC密度・地図の先読み可否）は
  `diffFor(level)` が **レベル値から自動算出**する。駅ごとに個別チューニングする必要はない。
- 駅の中の物理配置（階段・エスカレーターの位置、連絡通路、柵）は横浜駅版と同じ汎用エンジンを使い、
  実際の駅の正確な構造を再現したものではない（既存の免責と同じ）。
- 新しい駅を追加する場合は `STATIONS` に1件追記するだけでよい（レベルを1〜50の間で割り当てる）。

## 成績の保存（localStorage）
- `norikae-stats-v3`：通常モードの駅ごとの勝敗・最高マージン
- `norikae-trial-v1`：タイムトライアルの駅ごとの自己ベストタイム（秒）

## アクセス計測（analytics.js）
**これは広告SDKではない。** 「何人が遊んだか」を知るためだけの仕組みで、広告も決済も含まない
（下記「収益化の検討」の方針と矛盾しない）。GitHub Pagesはアクセスログを見られないため、これがないと
プレイ数がまったく分からず、収益化の判断ができない。

- 使うサービス：Umami Cloud（Cookieを使わないので同意バナー不要。無料枠 10万イベント/月）
- **`analytics.js` の `WEBSITE_ID` が空のあいだは、外部への通信も読み込みも一切行わない。**
  計測を始めるには、Umami Cloudで発行されたIDをこの定数に貼る
- やめたいときは `index.html` から `analytics.js` の読み込みを外すだけでよい

### 仕組み（ゲーム本体を汚さない）
`script.js` 側は `ev(name, props)` で `nk-ev` というCustomEventを投げるだけ。受信と送信は `analytics.js` が担当する。
`script.js` はUmamiの存在を知らないので、計測をやめてもゲームのコードに影響しない。

| イベント | いつ | 送る内容 |
|---|---|---|
| `mode_select` | モードを選んだとき | mode |
| `play_start` | `startGame()` | mode / level / station / crowd |
| `play_end` | `finish()` | mode / level / station / result（win・lose・goal）/ margin または seconds / zone（失敗した場所）|
| `retry` | 「もう一度」 | mode / level / station |
| `back_to_top` | 「駅・モードを選び直す」 | mode / level |

- 離脱率は `play_start` − `play_end` の差で計算する（離脱イベントは作らない。取りこぼしが多く不正確なため）
- **送るのはモード・レベル番号・駅ID・勝敗だけ。個人情報は送らない。**
  成績（localStorage）はサーバーに送らず、端末の中だけに置く

## 収益化の検討（無料提供が前提）
現状は広告SDK・決済SDKを組み込んでいない静的なPWA。実装コストが低い順に整理する。

1. **Web版（現状の配布形態）**
   - Google AdSense等のディスプレイ広告：個人開発の非公開PWAでは審査・収益ともに現実的ではない場合が多い。
     まずは note等での紹介記事からの流入経路を作り、遊んでもらう実績を作るのが先。
   - 「投げ銭」導線（note有料記事やBOOTHでの寄付ページ等）を結果画面や説明文に控えめに掲示する程度が無理のない範囲。
2. **スマホアプリ化後（本命）**
   - **リワード広告**：タイムトライアルの「もう1回だけ挑戦」やロック解除のヒント表示に対して視聴型広告（AdMob等）。
     ゲーム進行の必須要素にはしない（無理な広告視聴の強制はユーザー体験を損なう）。
   - **バナー／インタースティシャル広告**：結果画面の間に軽く挿入。頻度は控えめに。
   - **買い切りIAP「広告オフ」**：広告を消す一回購入。
   - **駅パックIAP**：「大阪版パック」「名古屋版パック」等、主要都市版の拡張駅セットを追加コンテンツとして販売。
   - 実装は将来のアプリ化（下記ロードマップ）が前提。現時点のWeb版には広告・決済コードは入れない
     （偽の広告枠や決済フォームを設置しない。ダミーUIはユーザーを誤解させるため作らない）。

## 今後の展望（ロードマップ）
- **駅ラインアップの拡充**：首都圏版をレベル1〜50でさらに拡充し、その後「主要都市版」（大阪・名古屋・福岡・札幌等）を
  追加パックとして展開。地方ごとに `STATIONS` を分割したデータファイルにする案も検討（現在は1ファイルに集約）。
- **スマホアプリ化**：まずは今のPWA（Capacitor等でWebViewをネイティブアプリ化）でiOS/Android配信を目指すのが最短。
  ストア審査対応・AdMob導入・IAP実装はこの段階で行う。将来的に描画負荷が増えた場合はネイティブUIへの移行も検討。
- **ランキング機能**：タイムトライアルの自己ベストは現状ローカル保存のみ。将来的にサーバー経由の全国ランキングを
  追加する場合は、個人情報を扱わないニックネーム方式などプライバシーに配慮した設計にする。

## 大事な約束
- 時刻・駅の配置・混雑・遅れはゲーム用の例。実際の時刻表や駅の構造とは違う（駅名・路線名は実在のものを使用）
- 説明の文章は、やさしい日本語で書く
- 成績はブラウザの localStorage に保存している
- Web版には広告・決済など収益化のコードを実装しない（上記「収益化の検討」は将来のアプリ化時の方針メモ）。
  ただし**アクセス計測（analytics.js）はこの制限の対象外**。広告でも決済でもなく、判断のための数字を取るだけ

## 収益化の進め方（2026-09 時点の計画）
前提：作業時間は**月5〜10時間**、初期費用は**0円**。この条件だと、0円で残る収益手段は投げ銭だけで、
それは**遊んだ人数がすべて**。だから「測る → 人を増やす → そこで初めて収益化を選ぶ」の順で進める。

```
[P0] 測る（済）   analytics.js を入れた。あとは Umami の WEBSITE_ID を入れるだけ
[P1] iframe対応   外部のゲーム投稿サイトに出せる形に直す
[P2] 配信先を増やす PLiCy（日本語・スマホ利用者が多い）が第一候補。itch.io が次点
[P3] 判断         下の基準に照らして決める
```

**判断の基準（数字を見る前に決めておく）**

| 月間プレイ開始数 | やること |
|---|---|
| 500未満 | 収益化はしない（何をしても誤差）。配信先を増やすか、撤退を考える |
| 500〜5,000 | 投げ銭の導線だけ改善する。それ以上は作らない |
| 5,000超 | ここで初めて Google Play 登録（約$25）に投資する価値が出る |

**撤退・縮小の基準（2つ以上当てはまったら）**：配信先を増やしても3ヶ月プレイ数が伸びない／自分が遊ばなくなった／
月の作業が10時間を超え続けている。**やめる判断は失敗ではない**（計測の仕組みと公開手順は次の制作物に使える）。
